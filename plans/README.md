# Plans index

## Active plans

Generated by the `improve` skill (accessibility-focused audit of header,
navigation, and footer) on 2026-07-24. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 014  | [Fix light-mode gold contrast failures in header/nav active states](014-fix-gold-contrast-light-mode-nav.md) | P1 | S | — | TODO |

### Findings considered and rejected

- Footer wordmark `text-gold` period (`Footer.tsx:43`) and the identical
  pattern in `style-guide/page.tsx`: not a finding — WCAG 1.4.3 exempts
  text that's part of a logo/brand-name treatment, and this measures the
  same as any other logotype flourish (~1.77:1, but decorative-exempt).
- Footer's scroll-to-bottom-on-focus behavior (`Footer.tsx:20-32`): flagged
  as a low-confidence UX/motion note during the audit, not a contrast or
  AA violation (it respects `prefers-reduced-motion`). Not planned;
  revisit only if a user reports disorientation.
- Footer column labels ("Visit", "Get in Touch", "Elsewhere") using `<p>`
  instead of headings: noted as a minor screen-reader-navigation
  nice-to-have, not planned — low impact, would need a broader look at
  the site's heading hierarchy to do well.

## Tech-debt & architecture batch (2026-08-07)

Generated by the `improve` skill (`/improve tech-debt` — tech-debt &
architecture audit only; security, performance, tests, dependencies, DX,
docs, and direction were out of scope for this pass). All six plans below
are independent of each other and of plan 014 — any order is fine, but
015 is the highest-leverage/lowest-risk and is a good place to start.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 015  | [Remove dead fields from site-config.ts and stop hardcoding the domain](015-dead-config-and-hardcoded-domain.md) | P1 | S | LOW | — | DONE |
| 016  | [Extract shared lightbox-orchestration hook from GalleryGrid and FeaturedWorkGrid](016-extract-lightbox-gallery-hook.md) | P1 | S | LOW | — | DONE |
| 017  | [Extract shared skeleton blocks from route loading.tsx files](017-dedupe-loading-skeletons.md) | P2 | S | LOW | — | DONE |
| 018  | [Share one validation schema between QuoteForm and /api/send](018-unify-quote-form-validation.md) | P1 | M | MED | — | DONE |
| 019  | [Remove duplicated/dead Turnstile widget-render logic in QuoteForm](019-dedupe-turnstile-render.md) | P2 | S | LOW | — | DONE (re-landed 2026-08-07 — original fix was stranded on an unmerged worktree branch, `main` never had it) |
| 020  | [Extract a shared, reference-counted body-scroll-lock hook](020-extract-body-scroll-lock-hook.md) | P2 | S | MED | — | DONE (re-landed 2026-08-07 — original fix sat uncommitted in a worktree, `main` never had it) |

Note: plan 019's DONE status was inaccurate until 2026-08-07 — the fix
(`fix: dedupe Turnstile widget render call in QuoteForm`) had been made on
an orphaned worktree branch (`claude/upbeat-goldwasser-e2a990`) that was
never merged into `main`, so `main` still carried both duplicate render
paths this whole time despite the row saying DONE. Re-applied directly
(the orphaned branch predated plan 018 and couldn't be merged as-is
without regressing the shared-schema validation). Lesson: a DONE row here
reflects that an executor reported success, not that the result reached
`main` — verify against `main` before trusting this table for anything
load-bearing.

Note: plan 015 directly resolves the hardcoded-domain risk called out under
"Remaining before launch" → "Deployment" below (`app/sitemap.ts` and
`app/robots.ts` hardcoding `https://antibroadcasting.com`) — once 015 lands,
that bullet can be struck.

### Findings considered and rejected

- `app/(site)/style-guide/prototype/` (static HTML/JSX design-exploration
  mockups, ~500KB, unreferenced by any route): looked like dead code on
  first pass, but it's a deliberate, already-documented decision — see
  `eslint.config.mjs`'s `globalIgnores` entry and its comment ("Static
  design-exploration mockups, never wired into app routing... not
  maintained as production code"). Settled, not a finding.
- `siteConfig.forms`/`seo`/`openGraph`/`twitter`/`fonts`/`analytics`/`legal`
  and the hardcoded-domain duplication in `robots.ts`/`sitemap.ts` are one
  finding each, not several — see plan 015, which addresses both together
  since they're the same root cause (a config object with dead fields
  whose one live field, `site.url`, wasn't consistently reused).

## Keystatic audit batch (2026-08-09)

Generated by the `improve` skill (`/improve` — focused audit of the
Keystatic CMS implementation: how it's used, correctness issues, and
opportunities to expand its capabilities/UX, per user request). Plans
021-025 are the numbered findings the user selected (1-5 of 7 surfaced);
026 is the GitHub-mode storage spike the user asked for separately under
"direction." Recommended order: 021 → 022 → 023 → 025 → 024 (024 depends on
022 and 023 landing first since it restructures the same schema region
those plans also touch) — 026 is independent and can run any time.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 021  | [Fix the live 404 behind the homepage promo's CTA button](021-fix-broken-promo-cta-link.md) | P1 | S | LOW | — | DONE — merged to `main` 2026-08-09 |
| 022  | [Use `fields.url`/pattern validation for link fields in `keystatic.config.ts`](022-validate-url-fields-in-keystatic-schema.md) | P2 | S | LOW | — | DONE — merged to `main` 2026-08-09 |
| 023  | [Replace "one item per line" textarea fields with `fields.array`](023-array-fields-for-list-content.md) | P2 | M | LOW-MED | — | DONE — merged to `main` 2026-08-09 (2nd execution attempt, after amending Step 6 for a readonly-array type mismatch found on the 1st) |
| 024  | [Group the `siteInfo` singleton's fields with `fields.object()`](024-group-siteinfo-fields.md) | P2 | S/M | LOW | 022, 023 | DONE — merged to `main` 2026-08-09 |
| 025  | [Extract shared, cached `lib/get-*.ts` readers for gallery/FAQ/art-requirements](025-shared-cached-collection-readers.md) | P2 | M | LOW | 023 | DONE — merged to `main` 2026-08-09 (executor ran before 023 landed and correctly used its STOP-condition fallback for `lib/get-art-requirements.ts`; fixed to the array form as part of merge conflict resolution once 023's schema landed alongside it) |
| 026  | [Spike: investigate and scaffold GitHub-mode Keystatic storage](026-spike-github-mode-storage-migration.md) | P3 | S | LOW | — | DONE — merged to `main` 2026-08-09 (still inert: no `KEYSTATIC_GITHUB_*` env vars set anywhere; see `docs/keystatic-github-mode-migration.md` for the manual cutover steps) |

All six merged into `main` via `--no-ff` merge commits, in dependency order
(021 → 022 → 023 → 025 → 024 → 026). Two merges (025, 024) hit conflicts —
all in files two branches both touched (`app/(site)/how-it-works/page.tsx`,
`keystatic.config.ts`, `content/site-info.json`, `lib/get-site-info.ts`) —
resolved by keeping the later/superseding plan's version in each case, plus
one real fix: `lib/get-art-requirements.ts` (from 025) had been written
against the pre-023 string-field schema since that executor ran before 023
landed; updated to the array form during conflict resolution. Full
verification after every merge step and again at the end:
`pnpm exec tsc --noEmit`, `pnpm test` (202 passed), `pnpm lint`, `pnpm build`
(14 routes, unchanged from baseline) — all clean. Not pushed to `origin/main`
yet.

### Findings considered and rejected

- Orphaned `public/gallery/gallery-item-number-two-copy/` directory (no
  matching content JSON file references it): real but low-value — a
  filesystem cleanup with no user-facing or code-correctness impact. Not
  planned; safe to delete opportunistically whenever someone is next in
  that directory.
- `lib/get-active-promo.ts`'s expiry filter/sort logic has no unit tests
  (pure function, easy to test, matches the existing `quote-request-schema.test.ts`
  convention): real test-coverage gap, but the user chose not to include it
  in this batch. Worth revisiting if `getActivePromo`'s sort/filter logic
  changes or a promo-scheduling bug is ever reported.

### Direction (not planned this round)

- **Bring page-level editorial copy into Keystatic** — hero headline/
  subhead, the `ProcessStrip` steps on the homepage, and the `CtaBand` copy
  are hardcoded JSX strings in `app/(site)/page.tsx` and
  `app/(site)/how-it-works/page.tsx` (two separate, drifting copies of
  similar "how it works" step content). Keystatic currently only owns
  structured list content plus one settings singleton, not page prose. No
  plan written — this is a bigger product/scope decision for the site
  owner, not a bounded implementation task.
- **Keystatic preview/draft mode** — editors currently can't preview an
  unpublished change against the real page layout before it's considered
  final. No plan written; flagged as a future UX enhancement once (or if)
  plan 026's GitHub-mode storage lands, since draft/preview matters more
  once edits aren't gated behind a local dev + manual redeploy cycle.

## Keystatic evolution batch (2026-08-10)

Generated by the `improve` skill (`/improve next` — direction-only audit of
Keystatic: how to evolve it for more capabilities and a critical UX review
beyond the 021-026 batch above). Both plans selected from the direction
findings; item 4 (preview/draft mode for unpublished pages) was explicitly
deferred to a later round per user decision — see 027's "Maintenance notes."

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 027  | [Add a guardrailed `pages` collection for one-off content](027-guardrailed-pages-collection.md) | P2 | L | MED | — | DONE — merged to `main` 2026-08-10 |
| 028  | [Rich-text promo descriptions + real alt-text fields](028-promo-description-and-alt-text.md) | P3 | M | LOW-MED | 027 (soft — see plan) | DONE — merged to `main` 2026-08-10 |

Both merged into `main` via `--no-ff` merge commits, 027 then 028 (no
conflicts — 028's `keystatic.config.ts` edits landed in different regions
than 027's). Notable executor finding during 027: `reader.collections.<name>.all()`
and `.read(slug)` do **not** share a return shape — `.all()` wraps each item
as `{ slug, entry: {...} }`, but `.read(slug)` returns the flat schema
object directly (`page.title`, not `page.entry.title`). The plan file was
amended mid-execution to correct this, and the merged code reflects the fix.
During 028, migrating the one real promo's `description` field through the
admin UI (per the plan's explicit instruction not to hand-author it)
produced a separate `content/promos/.../description.mdoc` file — confirms
`fields.document` splits into a per-entry content file even inside a
`format: { data: "json" }` collection; noted for future document-field
work. Full verification after merging both: `tsc` ✓, `pnpm test` (85
passed) ✓, `pnpm lint` (0 errors, 4 pre-existing unrelated warnings in
`Header.tsx`) ✓, `pnpm build` ✓ — 15 routes, `/updates/[slug]` now present
as SSG with zero prerendered pages (correct — no published `pages` entries
yet). Not pushed to `origin/main` yet.

Recommended order: 027 then 028 — not a hard code dependency, but 027
establishes the first working use of `fields.document` + `DocumentRenderer`
in this codebase, which 028 then reuses for `promos.description`.

Plan 028 executed ahead of 027 (soft dependency only, no conflict) — see
its status row above. One correction found during execution: plan 028's
"Current state" said `content/gallery/` has 6 entries; it has 5 (always
did, even at the plan's `d479ad9` commit — a miscount when the plan was
written, not drift). The executor applied `imageAlt: ""` to all 5 real
files; the plan's done-criteria grep expecting `6` correctly reads `5`
instead.

### Direction findings not selected this round

- GitHub-mode storage activation (env vars still unset — see plan 026 and
  `docs/keystatic-github-mode-migration.md`) — becomes more valuable once
  027 ships, since ad-hoc pages are exactly the content type someone might
  want to publish without a redeploy cycle. Not re-planned; still pending
  operator action (creating the GitHub OAuth App).
- Preview/draft mode for unpublished `pages` entries — deferred into 027's
  "Maintenance notes" as a documented future step, not built now.

## Production launch hardening (2026-08-13)

User is putting the site live at `https://antibroadcasting.vercel.app/`
(custom domain not attached yet) and asked to gate the previously-public
`/keystatic` admin UI ahead of that. This resolves the "Accepted, not a
launch blocker" note below — it's being fixed now that a live domain is
imminent, not staying accepted.

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 029  | [Password-gate the Keystatic admin UI before production launch](029-keystatic-admin-password-gate.md) | P1 | M | MED | — | DONE — 2026-08-13, not yet committed |

Executed directly (no separate worktree/branch — implemented, verified, and
manually walked through in the same session as planning). Built exactly as
scoped, with one on-the-fly correction: the plan specified `middleware.ts`,
but `pnpm build` immediately flagged it as deprecated in Next.js 16.2.9 in
favor of `proxy.ts` (file renamed to `proxy.ts`, exported function renamed
`middleware` → `proxy` to match). Full verification: `pnpm exec tsc --noEmit`
✓, `pnpm lint` ✓ (0 errors, pre-existing unrelated `MailIcon` warning only),
`pnpm test` ✓ (72 passed), `pnpm build` ✓ (`/keystatic-login` present as a
new route, no deprecation warning after the rename). Manually walked through
all three of plan 029's Step 5 behaviors in-browser with a temporary local
password (gate off → no redirect; gate on + wrong password → error shown;
gate on + right password → lands on `/keystatic`, persists across reload) —
`.env.local` reverted to the empty placeholder immediately after. Not
committed — waiting on the user's go-ahead per this repo's own git-safety
convention.

Plan 026's GitHub-mode storage activation (persistence — who can *save*) is
still separate, operator-only work, unchanged by 029 (which controls who
can *reach* `/keystatic` at all). See plan 029's "Why this matters" for how
the two relate. Note for whoever activates GitHub-mode next: the OAuth
callback URL in `docs/keystatic-github-mode-migration.md` currently targets
`antibroadcasting.vercel.app` (the site's launch domain, since
`antibroadcasting.com` isn't attached yet). GitHub OAuth Apps support only
one callback URL at a time — that field must be updated to the real domain
once it's live, not added alongside it.

## Launch checklist

All `improve`-skill plans (001-013) generated across the 2026-06-24 and
2026-06-28 sessions are complete, merged into `main`, and independently
re-verified as of 2026-06-29 — see "Verification" below. This file replaces
the per-plan tracking files, which have been removed now that nothing is
outstanding in code. What's left before launch is **operational**, not
code — listed below for reference.

## Verification (2026-06-29)

Ran fresh, with a cleared `.next` cache:

- `pnpm exec tsc --noEmit` → exit 0
- `pnpm build` → exit 0, 14 routes generated (including both edge-runtime
  routes, `/opengraph-image` and `/api/keystatic/[...params]`)
- `pnpm test` → 7/7 passed
- `pnpm lint` → exit 0
- `pnpm audit` → no known vulnerabilities
- Spot-checked each plan's core artifact directly in source: input
  validation schema, test file, `FileUpload` wiring, Keystatic storage
  config, all four Sentry config files, the CSP header, the `postcss`
  override, and the removal of the raw Resend error leak — all present and
  correct on `main`.
- `app/(site)/test-error/page.tsx` (a self-flagged debug page, *"Delete
  this page before deploying to production"*) has been removed.

## Remaining before launch — operational, not code

**Environment variables** — present as empty placeholders in `.env.local`
(or, for Sentry, not yet added); none are set as real values anywhere,
including Vercel:

- [ ] `RESEND_API_KEY` — get a real key from resend.com, **and** verify
  `quotes@antibroadcasting.com` (the `from` address in
  `content/site-info.json`) as a sending domain in Resend's dashboard.
  Unverified domains get emails rejected or quarantined.
- [ ] `NEXT_PUBLIC_TURNSTILE_SITE_KEY` / `TURNSTILE_SECRET_KEY` — create a
  site at Cloudflare's Turnstile dashboard. Until set, the honeypot field
  is the only spam defense live in production (Turnstile verification
  no-ops by design when the secret is unset).
- [ ] `NEXT_PUBLIC_SENTRY_DSN` — create a Sentry project, get a DSN. Until
  set, error capture stays dormant (errors still reach `console.error`/
  Vercel logs, just not Sentry).
- [ ] Set all three above as **Vercel project environment variables** —
  `.env.local` never ships; this is a separate step from having them
  locally.
- [ ] `KEYSTATIC_ADMIN_PASSWORD` — set once plan 029 lands, as a Vercel
  project environment variable (Production, and Preview if wanted). Until
  set, `/keystatic` and `/api/keystatic` stay fully public (the gate
  no-ops when unset, same convention as Turnstile).

**Deployment**

- [ ] Confirm the Vercel project is linked to `travhall/antibroadcasting`
  and `main` is the production branch.
- [ ] Confirm the custom domain (`antibroadcasting.com`) is attached with
  working DNS/SSL — `app/sitemap.ts` and `app/robots.ts` both read
  `siteConfig.site.url` (`lib/site-config.ts`) as of plan 015, so a domain
  change now only needs updating that one value.
- [ ] After the first production deploy with real keys: submit the quote
  form end-to-end (Turnstile renders and verifies, email arrives), and
  trigger a real error to confirm it lands in Sentry.

**Resolved** (previously "Accepted, not a launch blocker")

- Keystatic admin (`/keystatic`) ran in local storage mode with no auth
  layer of its own — accepted while there was no live production domain.
  Now that the site is going live at `antibroadcasting.vercel.app`, see
  "Production launch hardening" above (plan 029) — the admin UI is being
  password-gated via `proxy.ts` before launch (renamed from the initially-planned `middleware.ts` — Next.js 16 deprecates that filename in favor of `proxy.ts`, confirmed by the build's own deprecation warning during execution).

---

## Status update — 2026-08-24

Verified on `main` (clean working tree, up to date with origin):

- `pnpm exec tsc --noEmit` → exit 0
- `pnpm test --run` → **88/88 passed** (17 test files — grown from 7 at the June audit as email infrastructure tests were added)
- `pnpm audit` → **10 vulnerabilities (5 high, 5 moderate)** — new since June; both fix with package upgrades below.

### Packages to upgrade before deploying

- [ ] **`next` 16.2.9 → 16.2.11** — patches middleware/proxy bypass, DoS via Server Actions, SSRF in rewrites, cache confusion, and unbounded edge payload. Run `pnpm update next@latest` then verify `pnpm build` passes.
- [ ] **`sharp` → >=0.35.0** — 4 high libvips CVEs. Run `pnpm update sharp@latest`.

### Open question before wiring up RESEND_API_KEY

- [ ] **Which inbox receives quote submissions?** `emailTo` in `content/site-info.json` is `info@antibroadcasting.com`. Confirm with Chris before setting `RESEND_API_KEY` in Vercel.

Also note: the sending domain has changed since the June checklist. `emailFrom` is now `quotes@send.antibroadcasting.com` — verify the **`send.antibroadcasting.com` subdomain** in Resend's dashboard, not the root domain.

## Full audit batch (2026-09-04)

Generated by the `improve` skill (`/improve` — standard-effort full audit:
code quality, security, perf, tests, tech debt; also verified the
2026-08-24 status update's `next`/`sharp` upgrade blockers). Both flagged
package upgrades had already landed on `main` by this point (`next`
16.3.2, `sharp` 0.35.3) — no plan needed for those. Four new findings
selected by the user, all independent of each other and of everything
above; any order is fine, but 030 is worth doing first since it's the
one with the highest cost of staying broken (data loss risk, not just
code cleanliness).

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 030  | [Stop gitignoring plans/ and commit its existing history](030-track-plans-directory-in-git.md) | P1 | S | LOW | — | TODO |
| 031  | [Fix "Last updated" showing today's date on Privacy/Terms pages](031-fix-privacy-terms-last-updated-date.md) | P1 | S | LOW | — | TODO |
| 032  | [Remove stale git worktree and scope vitest away from it](032-remove-stale-worktree-and-scope-vitest.md) | P2 | S | LOW | — | TODO |
| 033  | [Override transitive fast-uri to patch 4 high-severity CVEs](033-override-fast-uri-cves.md) | P3 | S | LOW | — | TODO |

Note: plan 032 also explains a loose end in this file's own history — the
"88/88 passed, 17 files" figure recorded in the 2026-08-24 status update
above was inflated by the same stale-worktree double-counting bug plan 032
fixes. The real count at that time was roughly half those numbers.

### Findings considered and rejected

- `script-src 'self' 'unsafe-inline' ...` always allowing `'unsafe-inline'`
  (not just in dev) in `next.config.ts`'s CSP: common/expected for Next.js
  apps that don't wire up per-request nonces for hydration scripts — not a
  new finding, and changing it is a much larger CSP-nonce project than this
  audit's scope.
- `lib/keystatic-gate.ts`'s HMAC cookie being a fixed value per password
  (no expiry/session rotation): intentional bearer-token design for a
  single-admin-password gate, already reviewed as part of plan 029. Not
  re-flagged.
- `app/api/send/route.ts`'s in-memory per-IP rate limiter: already
  documented in-code as a deliberate Fluid-Compute-instance-reuse tradeoff.
  Not re-flagged.
